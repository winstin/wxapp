<block wx:if="{{$taroCompReady}}">
    <view class="{{styles.login}}">
        <image class="{{styles.logo}}" src="{{logo}}"></image>
        <view class="{{styles.title}}">请登录</view>
        <view class="{{styles.formItem}}">
            <image class="{{styles.itemIcon}}" src="{{phoneIcon}}"></image>
            <at-input compid="{{$compid__63}}"></at-input>
        </view>
        <view class="{{anonymousState__temp}}">
            <view class="{{styles.left}}">
                <image class="{{styles.itemIcon}}" src="{{codeIcon}}"></image>
                <at-input compid="{{$compid__64}}"></at-input>
            </view>
        </view>
        <at-button compid="{{$compid__65}}">登录</at-button>
        <view class="{{styles.bottom}}">
            <view class="{{styles.enterpriseName}}">苏州钛耘数据科技有限公司</view>
        </view>
    </view>
</block>