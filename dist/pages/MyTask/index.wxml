<block wx:if="{{$taroCompReady}}">
    <block wx:if="{{!token}}">
        <view class="{{styles.unLogin}}">
            <view>您还未登录，请先登录!</view>
            <view class="{{styles.receiveBtn}}" bindtap="anonymousFunc0">登录</view>
        </view>
    </block>
    <view class="{{styles.myTask}}" wx:else>
        <at-tabs compid="{{$compid__71}}">
            <at-tabs-pane wx:key="$loopState__temp2" wx:for="{{loopArray23}}" wx:for-item="item" wx:for-index="idx" compid="{{item.$compid__70}}">
                <scroll-view class="{{styles.tabContent}}" scroll-y="{{true}}" scroll-with-animation="{{true}}" scroll-top="{{scrollTop}}" style="{{item.$loopState__temp4}}" lower-threshold="{{Threshold}}" upper-threshold="{{Threshold}}" bindscrolltoupper="onScrollToUpper" bindscroll="onScroll">
                    <my-task-item wx:key="id" wx:for="{{item.$anonymousCallee__3}}" wx:for-item="task" wx:for-index="__index1" compid="{{task.$compid__69}}"></my-task-item>
                </scroll-view>
            </at-tabs-pane>
        </at-tabs>
    </view>
</block>